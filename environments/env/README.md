# env
> Please define the secret values in the **Azure Key Vault** resource by referring to the .env.example file. **KEY_VAULT_URL** must be provided as an environment variable (artifact) at container creation time.


# Swagger UI
> static files for air-gapped environment

## Third-Party License Notes
- Keep upstream Swagger UI license notices when redistributing bundled assets.
- If `swagger-ui-bundle.js` references `swagger-ui-bundle.js.LICENSE.txt`, include that file in the same directory.